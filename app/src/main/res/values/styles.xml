<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="TextTitle">
        <item name="android:textSize">22sp</item>
        <item name="android:textColor">@android:color/black</item>
    </style>
    <style name="CardView.ExternalCard">
        <item name="cardBackgroundColor">@color/grey</item>
        <item name="cardCornerRadius">5dp</item>
        <item name="cardElevation">2dp</item>
    </style>
    <style name="CardView.InternalCard">
        <item name="android:layout_margin">2dp</item>
        <item name="android:padding">5dp</item>
        <item name="android:background">@android:color/background_light</item>
    </style>

    <style name="ButtonBlue">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:background">@color/colorBtnBlue</item>
        <item name="android:padding">10dp</item>
    </style>

    <style name="ButtonOutline">
        <item name="android:background">@drawable/btn_outline_border</item>
    </style>

</resources>
